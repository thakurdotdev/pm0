{
  "name": "pm0-site",
  "version": "0.1.0",
  "private": true,
  "description": "Landing + docs site for pm0 (pm0.thakur.dev), served with Bun",
  "scripts": {
    "dev": "bun --hot ./server.ts",
    "start": "bun ./server.ts"
  }
}
